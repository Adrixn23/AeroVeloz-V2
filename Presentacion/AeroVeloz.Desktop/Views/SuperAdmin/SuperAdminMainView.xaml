<Window x:Class="AeroVeloz.Desktop.Views.SuperAdmin.SuperAdminMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:viewmodels="clr-namespace:AeroVeloz.Desktop.ViewModels.SuperAdmin"
        xmlns:views="clr-namespace:AeroVeloz.Desktop.Views.SuperAdmin"
        mc:Ignorable="d"
        Title="AeroVeloz - Super Admin" Height="720" Width="1280"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="14"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Name="MainAdminWindow">

        <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:AdminListViewModel}">
            <views:AdminListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:SuperAdminDashboardViewModel}">
            <views:SuperAdminDashboardView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AirportListViewModel}">
            <views:AirportListView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AirportDetailViewModel}">
            <views:AirportDetailView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:AdminDetailViewModel}">
            <views:AdminDetailView />
        </DataTemplate>
    </Window.Resources>

    <materialDesign:DialogHost Identifier="RootDialog" DialogTheme="Inherit">
        <Grid>
            <Grid.ColumnDefinitions>
                <!-- Sidebar -->
                <ColumnDefinition Width="280" />
                <!-- Main Content -->
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- SIDEBAR -->
            <Border Grid.Column="0" Background="{DynamicResource MaterialDesignPaper}" BorderBrush="{DynamicResource MaterialDesignDivider}" BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="160" /> <!-- User Profile / Branding -->
                        <RowDefinition Height="*" />   <!-- Navigation Menu -->
                        <RowDefinition Height="80" />  <!-- Footer / Logout -->
                    </Grid.RowDefinitions>

                    <!-- Branding / User Profile -->
                    <StackPanel Grid.Row="0" Margin="16" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <materialDesign:PackIcon Kind="ShieldCrown" Width="64" Height="64" Foreground="{DynamicResource PrimaryHueMidBrush}" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding UserName}" FontSize="18" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                        <TextBlock Text="{Binding UserRole}" FontSize="12" Foreground="Gray" HorizontalAlignment="Center" />
                    </StackPanel>

                    <!-- Navigation Menu -->
                    <StackPanel Grid.Row="1" Margin="0,16,0,0">
                        <Button Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Margin="8,4" Padding="16,8" Height="48" Command="{Binding NavigateToDashboardCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="ViewDashboard" Width="24" Height="24" Margin="0,0,16,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                <TextBlock Text="Dashboard" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Margin="8,4" Padding="16,8" Height="48" Command="{Binding NavigateToAirportsCommand}">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Airport" Width="24" Height="24" Margin="0,0,16,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                <TextBlock Text="Aeropuertos" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>

                        <Button Style="{StaticResource MaterialDesignFlatButton}" HorizontalContentAlignment="Left" Margin="8,4" Padding="16,8" Height="48" Command="{Binding NavigateToAdminsCommand}" ToolTip="Gestionar asignación de administradores a terminales">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="AccountTie" Width="24" Height="24" Margin="0,0,16,0" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                                <TextBlock Text="Administradores" VerticalAlignment="Center" FontSize="15" FontWeight="SemiBold"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>

                    <!-- Footer / Logout -->
                    <StackPanel Grid.Row="2" Margin="16" VerticalAlignment="Bottom">
                        <Button Style="{StaticResource MaterialDesignRaisedButton}" Command="{Binding LogoutCommand}" CommandParameter="{Binding ElementName=MainAdminWindow}" Background="#D32F2F" Foreground="White">
                            <StackPanel Orientation="Horizontal">
                                <materialDesign:PackIcon Kind="Logout" Margin="0,0,8,0"/>
                                <TextBlock Text="Cerrar Sesi�n" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- MAIN WORK AREA -->
            <Grid Grid.Column="1" Background="#F4F6F8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="64" /> <!-- App Bar -->
                    <RowDefinition Height="*" />  <!-- Dynamic Content -->
                </Grid.RowDefinitions>

                <!-- App Bar -->
                <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryMid" Padding="16" materialDesign:ElevationAssist.Elevation="Dp2">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                            <materialDesign:PackIcon Kind="Airplane" Width="36" Height="36" VerticalAlignment="Center" Margin="0,0,16,0"/>
                            <TextBlock Text="AeroVeloz - Panel Global" VerticalAlignment="Center" FontSize="20" FontWeight="Bold"/>
                        </StackPanel>
                        
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <TextBlock Text="{Binding SystemDate}" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.8"/>
                            <materialDesign:PopupBox PlacementMode="BottomAndAlignRightEdges" StaysOpen="False">
                                <materialDesign:PopupBox.ToggleContent>
                                    <materialDesign:PackIcon Kind="BellOutline" Width="28" Height="28"/>
                                </materialDesign:PopupBox.ToggleContent>
                                <StackPanel MinWidth="200" Margin="16">
                                    <TextBlock Text="Notificaciones del Sistema" FontWeight="Bold" Margin="0,0,0,8"/>
                                    <TextBlock Text="Todo se encuentra estable." Opacity="0.7"/>
                                </StackPanel>
                            </materialDesign:PopupBox>
                        </StackPanel>
                    </Grid>
                </materialDesign:ColorZone>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                    <ContentControl Content="{Binding CurrentViewModel}" Margin="16" />
                </ScrollViewer>
            </Grid>
        </Grid>
    </materialDesign:DialogHost>
</Window>
