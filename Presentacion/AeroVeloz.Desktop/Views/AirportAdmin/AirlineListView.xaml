<UserControl x:Class="AeroVeloz.Desktop.Views.AirportAdmin.AirlineListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="Transparent"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Loaded="UserControl_Loaded">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Aerolíneas en Conexión" FontSize="26" FontWeight="SemiBold" Foreground="#333333"/>
            <TextBlock Text="Listado de aerolíneas que tienen conexión con esta terminal." FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox Grid.Column="0" materialDesign:HintAssist.Hint="Buscar por nombre de aerolínea o código..." 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" 
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}" 
                     Margin="0,0,16,0" VerticalAlignment="Center"/>

            <Button Grid.Column="1" Style="{StaticResource MaterialDesignOutlinedButton}" Command="{Binding RefreshAirlinesCommand}" Padding="16,8" ToolTip="Refrescar">
                <StackPanel Orientation="Horizontal">
                    <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Refrescar" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     Value="0" IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,64,0,0" Height="50" Width="50"/>

        <!-- Data Grid -->
        <Border Grid.Row="2" Background="White" CornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp1" Visibility="{Binding IsNotBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <DataGrid ItemsSource="{Binding ConnectedAirlines}" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" SelectionMode="Single" materialDesign:DataGridAssist.CornerRadius="8" IsReadOnly="True" GridLinesVisibility="Horizontal" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Aerolínea" Binding="{Binding AirlineName}" Width="*"/>
                    <DataGridTextColumn Header="Código ICAO" Binding="{Binding CodeAirlinesIcao}" Width="150"/>
                    <DataGridCheckBoxColumn Header="Activo" Binding="{Binding IsActive, Mode=OneWay}" />
                    <DataGridTextColumn Header="Fecha de Registro" Binding="{Binding CreateAt, StringFormat='dd/MM/yyyy'}" Width="150" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>