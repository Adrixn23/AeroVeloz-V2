<UserControl x:Class="AeroVeloz.Desktop.Views.AirportAdmin.AuditLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Registro de Auditoría" FontSize="26" FontWeight="SemiBold" Foreground="#333333"/>
            <TextBlock Text="Consulta el historial de actividades en tu aeropuerto." FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Vertical" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBox Width="300" Padding="12,8" materialDesign:HintAssist.Hint="Buscar en auditoría..." 
                         Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Refrescar" 
                        Command="{Binding LoadAuditLogsCommand}" Margin="8,0,0,0" Padding="16,8"
                        Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Exportar" 
                        Command="{Binding ExportLogsCommand}" Margin="8,0,0,0" Padding="16,8"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                <TextBlock Text="Desde:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <DatePicker SelectedDate="{Binding FilterStartDate}" Width="150"/>
                <TextBlock Text="Hasta:" VerticalAlignment="Center" Margin="16,0,8,0"/>
                <DatePicker SelectedDate="{Binding FilterEndDate}" Width="150"/>
                <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Filtrar" 
                        Command="{Binding ApplyFiltersCommand}" Margin="16,0,0,0" Padding="16,8"
                        Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            </StackPanel>
        </StackPanel>

        <!-- Audit DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding AuditLogs}" SelectedItem="{Binding SelectedAudit}"
                  AutoGenerateColumns="True" CanUserAddRows="False" Background="White"
                  materialDesign:DataGridAssist.CellPadding="4" materialDesign:DataGridAssist.ColumnHeaderPadding="4"/>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     Value="0" IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     HorizontalAlignment="Center" VerticalAlignment="Center" Height="50" Width="50"/>
    </Grid>
</UserControl>

