<UserControl x:Class="AeroVeloz.Desktop.Views.AirportAdmin.AirportAdminDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:viewmodels="clr-namespace:AeroVeloz.Desktop.ViewModels.AirportAdmin"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             TextElement.Foreground="{DynamicResource MaterialDesignBody}"
             TextElement.FontWeight="Regular"
             TextElement.FontSize="14"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="Auto"
             Background="Transparent"
             FontFamily="{DynamicResource MaterialDesignFont}"
             Loaded="UserControl_Loaded">

    <Grid Margin="16" x:Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/> <!-- Quick Actions -->
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Control Operativo" FontSize="26" FontWeight="SemiBold" Foreground="#333333"/>
            <TextBlock Text="Métricas operativas de tu aeropuerto." FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="1" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     Value="0" IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,64,0,0" Height="50" Width="50"/>

        <!-- Stats Content -->
        <Grid Grid.Row="1" Visibility="{Binding IsNotBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="130"/>
                <RowDefinition Height="32"/>
                <RowDefinition Height="130"/>
            </Grid.RowDefinitions>

            <!-- Card 1: Aerolíneas Contactadas -->
            <materialDesign:Card Grid.Row="0" Grid.Column="0" Padding="24,16" UniformCornerRadius="8" Background="#1976D2" Foreground="White" materialDesign:ElevationAssist.Elevation="Dp3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="Airplane" Width="64" Height="64" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.8"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Aerolíneas en Contacto" FontSize="16" Opacity="0.9" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding AirportStats.ContactedAirlines, FallbackValue='0'}" FontSize="42" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Card 2: Operadores Total -->
            <materialDesign:Card Grid.Row="0" Grid.Column="2" Padding="24,16" UniformCornerRadius="8" Background="#0097A7" Foreground="White" materialDesign:ElevationAssist.Elevation="Dp3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="AccountMultiple" Width="64" Height="64" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.8"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Total de Operadores" FontSize="16" Opacity="0.9" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding AirportStats.TotalOperators, FallbackValue='0'}" FontSize="42" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Card 3: Conexiones Activas -->
            <materialDesign:Card Grid.Row="2" Grid.Column="0" Padding="24,16" UniformCornerRadius="8" Background="#00796B" Foreground="White" materialDesign:ElevationAssist.Elevation="Dp3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="Link" Width="64" Height="64" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.8"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Conexiones Activas" FontSize="16" Opacity="0.9" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding AirportStats.ActiveConnections, FallbackValue='0'}" FontSize="42" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>

            <!-- Card 4: Operaciones Pendientes -->
            <materialDesign:Card Grid.Row="2" Grid.Column="2" Padding="24,16" UniformCornerRadius="8" Background="#F57C00" Foreground="White" materialDesign:ElevationAssist.Elevation="Dp3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Kind="ClipboardCheck" Width="64" Height="64" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,16,0" Opacity="0.8"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right">
                        <TextBlock Text="Operaciones Pendientes" FontSize="16" Opacity="0.9" HorizontalAlignment="Right"/>
                        <TextBlock Text="{Binding AirportStats.PendingOperations, FallbackValue='0'}" FontSize="42" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </StackPanel>
                </Grid>
            </materialDesign:Card>
        </Grid>

        <!-- Quick Actions Pane -->
        <Border Grid.Row="2" Margin="0,48,0,0" Padding="24" Background="White" CornerRadius="8" materialDesign:ElevationAssist.Elevation="Dp1">
            <StackPanel>
                <TextBlock Text="Acciones Rápidas" FontSize="18" FontWeight="SemiBold" Foreground="#333333" Margin="0,0,0,16"/>
                <StackPanel Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Registrar Operador" Command="{Binding RegisterOperatorCommand}" Margin="0,0,16,0" Padding="16,8" Height="Auto" ToolTip="Agregar nuevo operador a tu aeropuerto"/>
                    <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Gestionar Conexiones" Command="{Binding ManageConnectionsCommand}" Margin="0,0,16,0" Padding="16,8" Height="Auto" ToolTip="Gestionar las conexiones de aerolíneas"/>
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Refrescar Indicadores" Command="{Binding RefreshIndicatorsCommand}" Margin="0,0,16,0" Padding="16,8" Height="Auto" Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
