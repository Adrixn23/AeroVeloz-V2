<UserControl x:Class="AeroVeloz.Desktop.Views.AirportAdmin.ConnectionListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="Transparent">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="Conexiones y Rutas" FontSize="26" FontWeight="SemiBold" Foreground="#333333"/>
            <TextBlock Text="Gestiona las rutas aéreas y conexiones." FontSize="14" Foreground="Gray"/>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox Width="300" Padding="12,8" materialDesign:HintAssist.Hint="Buscar conexión..." 
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Style="{StaticResource MaterialDesignOutlinedButton}" Content="Nueva Conexión" 
                    Command="{Binding CreateConnectionCommand}" Margin="16,0,0,0" Padding="16,8"/>
            <Button Style="{StaticResource MaterialDesignFlatButton}" Content="Refrescar" 
                    Command="{Binding LoadConnectionsCommand}" Margin="8,0,0,0" Padding="16,8"
                    Foreground="{DynamicResource PrimaryHueMidBrush}" Loaded="RefreshButton_Loaded"/>
        </StackPanel>

            <!-- Connections DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Connections}" SelectedItem="{Binding SelectedConnection}"
                  AutoGenerateColumns="False" CanUserAddRows="False" Background="White"
                  materialDesign:DataGridAssist.CellPadding="4" materialDesign:DataGridAssist.ColumnHeaderPadding="4">
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding ViewDetailsCommand}" CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
            </DataGrid.InputBindings>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="120"/>
                <DataGridTextColumn Header="Código ICAO Aerolínea" Binding="{Binding CodeAirlinesIcao}" Width="150"/>
                <DataGridTextColumn Header="Código ICAO Aeropuerto" Binding="{Binding CodeAirportIcao}" Width="150"/>
                <DataGridCheckBoxColumn Header="Activo" Binding="{Binding IsActive, Mode=OneWay}" Width="80"/>
                <DataGridTextColumn Header="Fecha de Creación" Binding="{Binding CreateAt, StringFormat='dd/MM/yyyy'}" Width="120"/>
                <DataGridTemplateColumn Header="Acciones" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Ver Detalles" 
                                        Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}" 
                                        Foreground="{DynamicResource PrimaryHueMidBrush}">
                                    <materialDesign:PackIcon Kind="Eye" />
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Editar" 
                                        Command="{Binding DataContext.EditConnectionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}" 
                                        Foreground="{DynamicResource PrimaryHueMidBrush}"
                                        Margin="4,0,0,0">
                                    <materialDesign:PackIcon Kind="Pencil" />
                                </Button>
                                <Button Style="{StaticResource MaterialDesignIconButton}" 
                                        ToolTip="Eliminar" 
                                        Command="{Binding DataContext.DeleteConnectionCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" 
                                        CommandParameter="{Binding}" 
                                        Foreground="#D32F2F" 
                                        Margin="4,0,0,0">
                                    <materialDesign:PackIcon Kind="Delete" />
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Loading Indicator -->
        <ProgressBar Grid.Row="2" Style="{StaticResource MaterialDesignCircularProgressBar}" 
                     Value="0" IsIndeterminate="True" 
                     Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}" 
                     HorizontalAlignment="Center" VerticalAlignment="Center" Height="50" Width="50"/>
    </Grid>
</UserControl>
